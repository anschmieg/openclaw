# Durable Memory Flush: 2026-02-21 (REDACTED)

This memory entry previously contained sensitive API keys and tokens which have been redacted and removed from repository history.

Redacted items:
- API keys, tokens, and secrets were removed and replaced with <REDACTED> placeholders.

Summary (sanitized):
- Model switches and deployments occurred; fixes were applied to OpenRouter routing and the TS gateway.
- Deployments were triggered and verification steps ran; some builds required full rebuilds.
- Provider work: Mistral provider added to config and models seeded; default agent set to mistral/codestral-latest.

If you need the original, short-lived values for auditing, retrieve them from the secure secrets manager or rotate and reissue new credentials.
